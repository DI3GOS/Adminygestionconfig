@model IEnumerable<WebApplication008.Models.ProfesorModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Materias Docente</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_materia_docente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_materia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_materia_docente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_materia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_usuario)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_materia_docente }) |
            @Html.ActionLink("Detalles", "Details", new { id = item.id_materia_docente }) |
            @Html.ActionLink("Borrar", "Delete", new { id = item.id_materia_docente })
        </td>
    </tr>
}

</table>
<div>
    @Html.ActionLink("Regresar al menu", "Index", "Home")
</div>
