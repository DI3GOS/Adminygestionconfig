@model IEnumerable<WebApplication008.Models.TrabajoModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Trabajos</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_materia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo_trabajo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.archivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_entrega)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.id_materia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_trabajo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.archivo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_entrega)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_trabajo }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_trabajo }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.id_trabajo })
        </td>
    </tr>
}

</table>
<div>
    @Html.ActionLink("Regresar al menu", "Index", "Home")
</div>